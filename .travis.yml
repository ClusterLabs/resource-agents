language: bash
sudo: false

addons:
  apt:
    #sources:
    #  - debian-sid
    packages:
      - libxml2-utils
      - xsltproc
      - shellcheck
script:
  - ./ci/build.sh
notifications:
  email: false
